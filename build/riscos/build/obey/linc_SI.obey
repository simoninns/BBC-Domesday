| -- build.obey.linc_SI - Domesday86 Acorn Archimedes build
| -- Linking module SI - State table initialisation
dir <$SRCDIR>.SI.c
Join stinit rcom rexam rgalwal rhe rnm rsear -to t1
Join scom sexam sgalwal she snm ssear -to t2
remove <$SRCDIR>.r.stinit
cdir <$SRCDIR>.r
Join <$HEADERDIR>.lib t1 t2 -to <$SRCDIR>.r.stinit
remove t1
remove t2
| -- Running r.stinit to build the state tables...
run <$SRCDIR>.r.stinit
